/**
 * Ejemplo de funcion inicial
 */

// Declaración de variable const
const world = '🗺️';
// Declaración de exportación de funcion
export function hello(word: string = world): string {
    return `Hello ${world}! `;
  }